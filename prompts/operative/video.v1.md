This is a video generation.

Work these into the description, only where the prompt leaves them open. They
are qualities of the sentence you write, not fields to list.

Shot size, from extreme close-up to wide, chosen for what the moment is about
rather than for variety. One camera move at most, with its direction named — a
locked-off frame is often stronger than an arbitrary drift, so choose stillness
deliberately when the subject carries the shot. A dolly has parallax and a zoom
does not; do not use the words interchangeably. The light by its source,
direction and hardness. Lens or depth of field only when it changes what the
viewer attends to.

Describe motion that can happen inside the clip's duration. A single continuous
beat, not a sequence of events.
